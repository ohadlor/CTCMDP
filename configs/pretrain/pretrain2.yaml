# @package _global_
# Pretrain with shrink factor
env:
  shrink_factor: 0.25
experiment_name: "025"

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      agent: choice(tc_m2td3/vanilla, tc_m2td3/stacked, tc_m2td3/oracle)
