# @package _global_
# Pretrain with no shrink factor
env:
  shrink_factor: 0
experiment_name: ${env.shrink_factor}

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      agent: choice(td3, tc_m2td3/vanilla, tc_m2td3/stacked, tc_m2td3/oracle)
