# @package _global_
defaults:
  - /schedule: brownian

agent:
  variant: vanilla
  model_based: true

hydra:
  sweep:
    dir: experiments/${hydra.job.config_name}
    subdir: ${model.radius}_${model._name_}