# @package _global_
# Randomize initial hidden state, static schedule, (maybe use rrls built in method)

experiment_name: stationary
schedule: static

hydra:
  sweeper:
    params:
      agent: choice(td3, vanilla_tc_m2td3, stacked_tc_m2td3, oracle_tc_m2td3, continual_td3, pretrainted_continual_td3.td3, pretrainted_continual_td3.tc_m2td3)