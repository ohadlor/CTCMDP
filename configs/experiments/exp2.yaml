# @package _global_
# Sweep over radius and sim discount factor

experiment_name: discount_radius
hydra:
  sweeper:
    params:
      agent.radius: ${logspace:0.0001,0.5,20}
      ++agent.model.sim_discount_factor: ${linspace:0,1,20}
