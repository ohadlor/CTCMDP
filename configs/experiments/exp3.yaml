# @package _global_
# sweep over schedules

experiment_name: shrink_factor
hydra:
  sweeper:
    params:
      agent: choice(tc_m2td3/vanilla, tc_m2td3/stacked, tc_m2td3/oracle, continual_td3/tc_m2td3)

agent:
  boot_with_shrink_factor: true
