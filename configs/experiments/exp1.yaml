# @package _global_
# sweep over schedules

experiment_name: schedules
hydra:
  sweeper:
    params:
      schedule: choice(brownian, linear, sinusoidal, static)
      agent: choice(td3, vanilla_tc_m2td3, stacked_tc_m2td3, oracle_tc_m2td3, continual_td3, pretrained_continual_td3/td3, pretrained_continual_td3/tc_m2td3)
